[project]
name = "mcp-clients"
version = "0.1.0"
description = "MCP client utilities"
requires-python = ">=3.11"
dependencies = ["mcp>=1.20.0"]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
